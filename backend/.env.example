# Campus Hub — Backend environment

PORT=5000
NODE_ENV=development
JWT_SECRET=change-me-to-a-long-random-string

# PostgreSQL
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=CampusHUb
DB_USER=postgres
DB_PASSWORD=your-local-password

# Optional: YouTube Data API key for importing playlists (single video links work without this)
# YOUTUBE_API_KEY=your-google-api-key
# or rely on these DB_* variables (preferred).
